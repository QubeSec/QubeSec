---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: quantumcertificates.qubesec.io
spec:
  group: qubesec.io
  names:
    kind: QuantumCertificate
    listKind: QuantumCertificateList
    plural: quantumcertificates
    shortNames:
    - qc
    singular: quantumcertificate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .spec.algorithm
      name: Algorithm
      type: string
    - jsonPath: .spec.domain
      name: Domain
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: QuantumCertificate is the Schema for the quantumcertificates
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: QuantumCertificateSpec defines the desired state of QuantumCertificate
            properties:
              algorithm:
                description: Foo is an example field of QuantumCertificate. Edit quantumcertificate_types.go
                  to remove/update
                type: string
              days:
                type: integer
              domain:
                type: string
              secretName:
                description: Optional name of the Secret to store certificate and
                  key. Defaults to resource name.
                type: string
            type: object
          status:
            description: QuantumCertificateStatus defines the observed state of QuantumCertificate
            properties:
              certificateFingerprint:
                description: CertificateFingerprint is a hash of the certificate for
                  verification (hex-encoded)
                type: string
              certificateReference:
                description: CertificateReference points to where the certificate
                  is stored
                properties:
                  name:
                    description: Name of the referent
                    type: string
                  namespace:
                    description: Namespace of the referent; empty defaults to current
                      namespace
                    type: string
                required:
                - name
                type: object
              error:
                description: Error message if generation failed
                type: string
              lastUpdateTime:
                description: LastUpdateTime is when the certificate was last generated
                format: date-time
                type: string
              status:
                description: Status of certificate generation
                enum:
                - Pending
                - Success
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
