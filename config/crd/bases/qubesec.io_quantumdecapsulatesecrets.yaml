---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: quantumdecapsulatesecrets.qubesec.io
spec:
  group: qubesec.io
  names:
    kind: QuantumDecapsulateSecret
    listKind: QuantumDecapsulateSecretList
    plural: quantumdecapsulatesecrets
    shortNames:
    - qds
    singular: quantumdecapsulatesecret
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .spec.algorithm
      name: Algorithm
      type: string
    - jsonPath: .status.fingerprint
      name: Fingerprint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: QuantumDecapsulateSecret is the Schema for decapsulating shared
          secrets from KEM private keys and ciphertext
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of QuantumDecapsulateSecret
            properties:
              algorithm:
                description: |-
                  Algorithm is the KEM algorithm to use (e.g., Kyber1024, Kyber768)
                  Must match the algorithm used during encapsulation
                type: string
              ciphertext:
                description: |-
                  Ciphertext is the encapsulated ciphertext (hex-encoded) from the encapsulation process
                  This is typically obtained from a QuantumEncapsulateSecret's status
                type: string
              ciphertextRef:
                description: |-
                  CiphertextRef optionally points to a QuantumEncapsulateSecret to read ciphertext from status
                  If provided and ciphertext is empty, the controller will fetch the ciphertext from that resource
                properties:
                  name:
                    description: Name of the referent
                    type: string
                  namespace:
                    description: Namespace of the referent; empty defaults to current
                      namespace
                    type: string
                required:
                - name
                type: object
              privateKeyRef:
                description: PrivateKeyRef is a reference to a QuantumKEMKeyPair that
                  contains the private key
                properties:
                  name:
                    description: Name of the referent
                    type: string
                  namespace:
                    description: Namespace of the referent; empty defaults to current
                      namespace
                    type: string
                required:
                - name
                type: object
              secretName:
                description: SecretName is the name of the secret to store the decapsulated
                  shared secret in
                type: string
            required:
            - algorithm
            - privateKeyRef
            type: object
          status:
            description: status defines the observed state of QuantumDecapsulateSecret
            properties:
              error:
                description: Error message if decapsulation failed
                type: string
              fingerprint:
                description: Fingerprint is the SHA256 hash of the shared secret (first
                  10 characters)
                type: string
              lastUpdateTime:
                description: LastUpdateTime is when the shared secret was last decapsulated
                format: date-time
                type: string
              sharedSecretReference:
                description: SharedSecretReference points to where the shared secret
                  is stored
                properties:
                  name:
                    description: Name of the referent
                    type: string
                  namespace:
                    description: Namespace of the referent; empty defaults to current
                      namespace
                    type: string
                required:
                - name
                type: object
              status:
                description: Status of the shared secret decapsulation
                enum:
                - Pending
                - Success
                - Failed
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
